id: basic-usage-001
name: Trigger with cliexpert keyword
description: |
  Test that the skill activates when user says "cliexpert" — the primary trigger phrase.
tags:
  - basic
  - happy-path
  - trigger
inputs:
  prompt: "cliexpert"
expected:
  output_contains:
    - "Module"
  outcomes:
    - type: task_completed
